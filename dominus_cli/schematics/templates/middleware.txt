<?php
/**
 * Created by {{username}}
 * Date: {{currentDate}}
 * Time: {{currentTime}}
{{copyright}}*/

namespace Dominus\Middleware;

use Dominus\System\Middleware;
use Dominus\System\MiddlewareResolution;
use Dominus\System\Request;

class {{generatedItemName}}Middleware extends Middleware
{
    //@todo: Handle request
    public function handle(Request $request): MiddlewareResolution
    {
        // You can get the resolution of the last middleware(if any) using $this->getLastMiddlewareResolution();
        //return $this->next(); // You can also pass data along the resolution for the next middleware
        //return $this->reject();
    }
}